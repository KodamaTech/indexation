import { Text } from "@chakra-ui/react";

function Description() {
	return (
		<Text mt="4" fontSize="xl">
			Specify the parameters and we will calculate the indexation amount for the
			current day, taking into account inflation
		</Text>
	);
}

export default Description;
